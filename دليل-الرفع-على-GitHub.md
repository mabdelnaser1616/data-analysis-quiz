# دليل رفع المشروع على GitHub - خطوة بخطوة 📚

## الخطوة 1: إنشاء حساب GitHub (إذا لم يكن لديك حساب)

1. اذهب إلى [github.com](https://github.com)
2. اضغط "Sign up"
3. املأ البيانات وأنشئ الحساب
4. تحقق من البريد الإلكتروني

---

## الخطوة 2: إنشاء مستودع جديد على GitHub

1. سجل دخول إلى GitHub
2. اضغط على زر **"+"** في أعلى الصفحة (بجانب صورتك)
3. اختر **"New repository"**
4. املأ البيانات:
   - **Repository name:** `data-analysis-quiz` (أو أي اسم تريده)
   - **Description:** `موقع اختبار تحليل البيانات مع ChatGPT و D-ID`
   - **Visibility:** اختر **Public** (أو Private إذا كنت تريد خصوصية)
   - **⚠️ مهم:** لا تضع علامة على "Add a README file" أو "Add .gitignore"
5. اضغط **"Create repository"**

---

## الخطوة 3: تثبيت Git على جهازك (إذا لم يكن مثبتاً)

### للتحقق من وجود Git:
افتح PowerShell أو Command Prompt واكتب:
```bash
git --version
```

### إذا لم يكن مثبتاً:

#### Windows:
1. اذهب إلى [git-scm.com/download/win](https://git-scm.com/download/win)
2. حمّل الملف وثبته
3. أثناء التثبيت، اختر جميع الخيارات الافتراضية

#### أو استخدم Chocolatey:
```bash
choco install git
```

---

## الخطوة 4: إعداد Git على جهازك (للمرة الأولى فقط)

افتح PowerShell في مجلد المشروع (`D:\dol2`) واكتب:

```bash
git config --global user.name "اسمك"
git config --global user.email "بريدك@example.com"
```

**مثال:**
```bash
git config --global user.name "Ahmed Mohamed"
git config --global user.email "ahmed@example.com"
```

---

## الخطوة 5: تهيئة Git في مجلد المشروع

افتح PowerShell في مجلد المشروع (`D:\dol2`) واكتب:

```bash
git init
```

سترى رسالة: `Initialized empty Git repository in D:/dol2/.git/`

---

## الخطوة 6: إضافة الملفات إلى Git

### أ) إضافة جميع الملفات:
```bash
git add .
```

### ب) للتحقق من الملفات المضافة:
```bash
git status
```

سترى قائمة بجميع الملفات التي ستُرفع.

---

## الخطوة 7: عمل Commit (حفظ التغييرات)

```bash
git commit -m "Initial commit - موقع اختبار تحليل البيانات"
```

**ملاحظة:** إذا ظهرت رسالة خطأ عن البريد الإلكتروني، راجع الخطوة 4.

---

## الخطوة 8: ربط المشروع مع GitHub

### أ) احصل على رابط المستودع:
1. اذهب إلى صفحة المستودع على GitHub
2. اضغط زر **"Code"** (أخضر)
3. انسخ الرابط (HTTPS) - مثل: `https://github.com/yourusername/data-analysis-quiz.git`

### ب) أضف المستودع البعيد:
```bash
git remote add origin https://github.com/yourusername/data-analysis-quiz.git
```

**⚠️ استبدل `yourusername` و `data-analysis-quiz` بروابطك الفعلية**

### ج) للتحقق:
```bash
git remote -v
```

يجب أن ترى رابطين (fetch و push).

---

## الخطوة 9: رفع الملفات على GitHub

```bash
git push -u origin main
```

**إذا ظهرت رسالة خطأ عن "main" vs "master":**
```bash
git branch -M main
git push -u origin main
```

**إذا طُلب منك اسم المستخدم وكلمة المرور:**
- اسم المستخدم: اسمك على GitHub
- كلمة المرور: **Personal Access Token** (ليس كلمة المرور العادية)

### كيفية إنشاء Personal Access Token:
1. اذهب إلى GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. اضغط "Generate new token"
3. اختر الصلاحيات: `repo` (كل الصلاحيات)
4. اضغط "Generate token"
5. انسخ الرمز (سيظهر مرة واحدة فقط!)
6. استخدمه ككلمة مرور عند `git push`

---

## الخطوة 10: التحقق من النجاح

1. اذهب إلى صفحة المستودع على GitHub
2. يجب أن ترى جميع الملفات:
   - `index.html`
   - `script.js`
   - `style.css`
   - `package.json`
   - `api/` (مجلد)
   - وغيرها...

---

## الملفات التي ستُرفع ✅

### ملفات أساسية:
- ✅ `index.html` - الصفحة الرئيسية
- ✅ `script.js` - الكود البرمجي
- ✅ `style.css` - التصميم
- ✅ `package.json` - معلومات المشروع
- ✅ `vercel.json` - إعدادات Vercel
- ✅ `README.md` - ملف الشرح
- ✅ `VERCEL_DEPLOY.md` - دليل النشر

### مجلدات:
- ✅ `api/` - Serverless functions لـ Vercel
  - `generate-video.js`
  - `video-status.js`
  - `did/presenters.js`

### ملفات مساعدة:
- ✅ `d-id-helper.html`
- ✅ `get-did-presenters.html`
- ✅ `test-did-elevenlabs.html`
- ✅ `INSTALL.md`
- ✅ `.env.example`

---

## الملفات التي لن تُرفع ❌

(بسبب `.gitignore`):
- ❌ `node_modules/` - مجلد المكتبات (سيعاد تثبيته على Vercel)
- ❌ `.env` - ملف المتغيرات البيئية (للمحلية فقط)
- ❌ `.DS_Store` - ملفات النظام
- ❌ `*.log` - ملفات السجلات
- ❌ `.vercel` - إعدادات Vercel المحلية

---

## استكشاف الأخطاء 🔧

### خطأ: "fatal: not a git repository"
**الحل:** تأكد أنك في المجلد الصحيح وأنك نفذت `git init`

### خطأ: "Please tell me who you are"
**الحل:** نفذ الخطوة 4 (إعداد Git)

### خطأ: "remote origin already exists"
**الحل:**
```bash
git remote remove origin
git remote add origin https://github.com/yourusername/repo.git
```

### خطأ: "Authentication failed"
**الحل:** استخدم Personal Access Token بدلاً من كلمة المرور

### خطأ: "main branch does not exist"
**الحل:**
```bash
git branch -M main
git push -u origin main
```

---

## الخطوات التالية 🚀

بعد رفع المشروع على GitHub بنجاح:
1. ✅ راجع `VERCEL_DEPLOY.md` للخطوة التالية (رفع على Vercel)
2. ✅ تأكد من أن جميع الملفات موجودة على GitHub
3. ✅ تحقق من أن `.gitignore` يعمل بشكل صحيح

---

## نصائح 💡

1. **Commits منتظمة:** بعد أي تغيير، استخدم:
   ```bash
   git add .
   git commit -m "وصف التغيير"
   git push
   ```

2. **التحقق من الحالة:**
   ```bash
   git status
   ```

3. **رؤية التاريخ:**
   ```bash
   git log
   ```

---

**🎉 تهانينا! المشروع الآن على GitHub!**

