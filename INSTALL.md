# دليل التثبيت والتشغيل 📦

## المشكلة الحالية
المتصفح يمنع الاتصال المباشر بـ Heygen API بسبب CORS. الحل هو استخدام خادم backend بسيط.

## الخطوات:

### 1. تثبيت Node.js
إذا لم يكن Node.js مثبتاً:
- حمّل من: https://nodejs.org
- ثبت الإصدار LTS

### 2. تثبيت المكتبات
افتح Terminal في مجلد المشروع وشغّل:
```bash
npm install
```

### 3. تشغيل الخادم
شغّل أحد الخيارات:

**الطريقة 1: استخدام ملف البات**
- اضغط نقرتين على `start-server.bat`

**الطريقة 2: من Terminal**
```bash
npm start
```

### 4. فتح الموقع
بعد أن ترى "Server running on http://localhost:3000":
- افتح `index.html` في المتصفح
- أو افتح `http://localhost:3000` مباشرة

## ملاحظات مهمة:

1. **الخادم يجب أن يعمل دائماً** عند استخدام ميزة الفيديو
2. **لا تغلق Terminal** الذي يعمل فيه الخادم
3. إذا غيرت `server.js`، أعد تشغيل الخادم

## استكشاف الأخطاء:

### الخطأ: "npm is not recognized"
- Node.js غير مثبت أو غير موجود في PATH
- أعد تثبيت Node.js

### الخطأ: "Port 3000 already in use"
- البورت 3000 مستخدم
- غير البورت في `server.js` من 3000 إلى رقم آخر (مثل 3001)

### الخطأ: "Cannot find module"
- شغّل `npm install` مرة أخرى

## الملفات المهمة:

- `server.js` - الخادم الذي يتصل بـ Heygen API
- `package.json` - قائمة المكتبات المطلوبة
- `script.js` - كود الموقع (يستخدم الخادم الآن)

---

بعد تشغيل الخادم، كل شيء سيعمل! 🚀

